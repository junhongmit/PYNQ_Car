#ifdef __cplusplus
extern "C" {
#endif
#include "xio_switch.h"
#ifdef __cplusplus
}
#endif

#include "gpio.h"

int main()
{
	set_pin(0, GPIO);
	gpio_open(0);
	gpio_set_direction(0, GPIO_OUT);
	while (1) {
		gpio_write(0, 0);
		gpio_write(0, 1);
	}
	return 0;
}
